プレビュー機能ありがとうございます。amber ハイライトと `useState` でのリアルタイム更新は仕様書 §6 通りで、Server Action はちゃんと props 経由で渡していて良いと思いました。

何点か対応お願いしたいです:

## 🔴 必須: PR #28 とのファイル重複

このPRには PR #28 と完全に同一の3ファイル (+ package-lock) が含まれています:

- `lib/utils/render-template.ts`
- `lib/utils/render-template.test.ts`
- `package.json` (Vitest 追加部分)

PR #28 が main にマージ済みなので、このブランチを最新の main に rebase すれば diff から自動的に消えるはずです:

```bash
git checkout 11-stage5-template-preview
git fetch origin main
git rebase origin/main
git push --force-with-lease
```

base ブランチも `11-stage3-templates-ui` から `main` に変更してください（GitHub UIの右上「Edit」→「base」）。**PR #27 マージ後に rebase** すると一番きれいになります。

## 🟡 推奨: parseSegments の重複実装

template-form.tsx の `parseSegments` は `lib/utils/render-template.ts` の `renderTemplate` と同じ regex を再実装しています。仕様変更時に2箇所直す必要があるので、`render-template.ts` に `parseSegments` を export してこちらから import する形が良いです:

```ts
// lib/utils/render-template.ts
export type Segment =
  | { kind: 'text'; value: string }
  | { kind: 'placeholder'; name: string }

export function parseSegments(template: string): Segment[] { ... }

// components/features/template-form.tsx
import { parseSegments, type TemplateVars } from '@/lib/utils/render-template'
```

## 🟢 軽微: アクセシビリティ

プレビュー領域に `aria-label="プレビュー"` か `<section aria-labelledby="...">` を付けると screen reader フレンドリーです。

---

#27 と #28 が main に入った後、rebase + 上記修正お願いします 🙏
